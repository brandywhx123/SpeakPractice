# 中英文发音训练助手 — 交付说明文档

## 一、项目概述

| 项 | 值 |
|---|---|
| 项目名称 | 中英文发音训练助手 (EnCnVoice) |
| 应用包名 | com.example.pronunciationassistant |
| 开发语言 | Kotlin |
| 最低系统 | Android 7.0 (API 24) |
| 目标系统 | Android 15 (API 36) |
| 构建工具 | Gradle 8.9 + AGP 8.7.2 |
| Kotlin 版本 | 2.0.21 |
| JDK 版本 | 17 |
| APK 大小 | 约 6.8 MB (debug) |

应用核心功能是通过对比用户朗读的发音与标准文本，分析发音内容的正确性，并给出正确率百分比反馈。支持**中文**和**英文**双语训练。

---

## 二、功能清单

### 2.1 核心功能

| 功能 | 实现方式 | 说明 |
|---|---|---|
| 文本输入 | EditText (TextInputLayout) | 用户输入待练习的单词或句子 |
| 语言选择 | RadioGroup (英文/中文) | 切换 TTS 和语音识别的目标语言 |
| 播放标准音 | Android TextToSpeech | 根据语言选择播放对应语种的标准发音 |
| 发音训练 | RecognizerIntent | 调用系统语音识别录音并转为文本 |
| 文本对比 | Levenshtein 编辑距离 | 动态规划算法计算识别文本与标准文本差异 |
| 结果反馈 | TextView | 显示标准文本、识别结果、编辑距离、正确率 |
| 权限管理 | 动态申请 RECORD_AUDIO | Android 6.0+ 运行时权限申请 |

### 2.2 双语支持

| 模块 | 英文模式 | 中文模式 |
|---|---|---|
| TTS 语音合成 | `Locale.US` | `Locale.SIMPLIFIED_CHINESE` |
| 语音识别 (STT) | `EXTRA_LANGUAGE = "en-US"` | `EXTRA_LANGUAGE = "zh-CN"` |
| 文本对比算法 | Levenshtein 编辑距离 (字符级) | 同左 (按 Unicode 字符计算) |

---

## 三、项目结构

```
EnCnVoice/
├── settings.gradle                    # Gradle 项目配置
├── build.gradle                       # 项目级构建脚本 (AGP 8.7.2 / Kotlin 2.0.21)
├── gradle.properties                  # Gradle 属性配置
├── local.properties                   # SDK 路径配置 (本机路径，不入版本控制)
├── gradlew                            # Unix Gradle 包装脚本
├── gradlew.bat                        # Windows Gradle 包装脚本
├── gradle/wrapper/
│   ├── gradle-wrapper.jar             # Gradle Wrapper JAR (43 KB)
│   └── gradle-wrapper.properties      # Gradle 8.9 下载配置
├── app-debug.apk                      # 编译产物 (debug，6.8 MB)
└── app/
    ├── build.gradle                   # 应用级构建脚本 (compileSdk 36)
    ├── proguard-rules.pro             # ProGuard 规则 (空)
    └── src/main/
        ├── AndroidManifest.xml        # 应用清单 + 权限声明
        ├── java/com/example/pronunciationassistant/
        │   └── MainActivity.kt        # 核心代码 (TTS + 语音识别 + 编辑距离)
        └── res/
            ├── layout/activity_main.xml   # 主界面布局 (垂直布局)
            ├── values/strings.xml          # 字符串资源
            └── mipmap/
                ├── ic_launcher.xml        # 应用图标 (矢量)
                └── ic_launcher_round.xml  # 圆形图标 (矢量)
```

---

## 四、核心技术实现

### 4.1 TTS 语音合成 (TextToSpeech)

**初始化流程：**
1. `TextToSpeech(this, this)` 创建引擎实例，传入 `OnInitListener` 回调
2. `onInit()` 回调中确认初始化成功后设置默认语言 `Locale.US`
3. 播放时根据语言选择动态调用 `tts.setLanguage()` 切换语言
4. `tts.speak(text, QUEUE_FLUSH, null, null)` 朗读文本

**关键代码位置：** MainActivity.kt `initTTS()` / `onInit()` / `btnPlay.setOnClickListener`

### 4.2 语音识别 (SpeechRecognizer / RecognizerIntent)

**识别流程：**
1. 检查并动态申请 `RECORD_AUDIO` 权限
2. 创建 `Intent(RecognizerIntent.ACTION_RECOGNIZE_SPEECH)`
3. 配置参数：
   - `EXTRA_LANGUAGE_MODEL`: `LANGUAGE_MODEL_FREE_FORM` (自由文本模式)
   - `EXTRA_LANGUAGE`: 根据语言选择 `"en-US"` 或 `"zh-CN"`
   - `EXTRA_MAX_RESULTS`: 1 (取置信度最高的一条)
4. `startActivityForResult()` 启动系统语音识别界面
5. `onActivityResult()` 中从 `EXTRA_RESULTS` 获取识别文本

**关键代码位置：** MainActivity.kt `startSpeechRecognition()` / `onActivityResult()`

### 4.3 文本对比算法 (Levenshtein 编辑距离)

**算法原理：**
编辑距离是指将一个字符串转换成另一个字符串所需的最少单字符编辑操作次数。允许三种操作：
- 插入 (Insert)
- 删除 (Delete)
- 替换 (Replace)

**动态规划实现：**
- 构建 DP 二维表 `dp[i][j]`，表示 `s1[0..i-1]` 与 `s2[0..j-1]` 的编辑距离
- 状态转移方程：
  - 字符相同：`dp[i][j] = dp[i-1][j-1]`
  - 字符不同：`dp[i][j] = 1 + min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1])`
- 边界条件：`dp[i][0] = i`，`dp[0][j] = j`
- 时间复杂度：O(m × n)，空间复杂度：O(m × n)

**正确率计算公式：**
```
正确率 = (1 - 编辑距离 / 标准文本长度) × 100%
```

**中英文通用性说明：**
算法基于 Unicode 字符级别。Kotlin `String.length` 对中文字符按字符数计算（如 "你好" 长度为 2），因此同一套算法无需修改即可同时支持中英文。

**关键代码位置：** MainActivity.kt `levenshteinDistance()` / `calculateAccuracy()`

### 4.4 权限管理

**静态声明 (AndroidManifest.xml)：**
```xml
<uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-feature android:name="android.hardware.microphone" android:required="false" />
```

**动态申请 (代码)：**
- `ContextCompat.checkSelfPermission()` 检查权限状态
- `ActivityCompat.requestPermissions()` 弹出系统授权对话框
- `onRequestPermissionsResult()` 处理授权结果

**关键代码位置：** MainActivity.kt `checkAndRequestRecordAudioPermission()` / `onRequestPermissionsResult()`

---

## 五、UI 界面设计

界面采用垂直布局 (Vertical Layout)，自上而下排列：

```
┌───────────────────────────────────┐
│        中英文发音训练助手           │  标题 (24sp, 蓝色)
├───────────────────────────────────┤
│  训练语言:  ◉ 英文   ○ 中文       │  RadioGroup 语言选择
├───────────────────────────────────┤
│  [请输入待练习的单词或句子]         │  EditText 文本输入框
│                                   │
├───────────────────────────────────┤
│       [    播放标准音    ]         │  Button (蓝色 #2196F3)
├───────────────────────────────────┤
│      [  开始发音训练  ]            │  Button (绿色 #4CAF50)
├───────────────────────────────────┤
│                                   │
│      等待训练...                   │  TextView 结果显示区
│      (灰底 #F5F5F5)               │  (初始状态)
│                                   │
└───────────────────────────────────┘
```

**结果显示区输出示例：**
```
标准文本：Hello World
识别结果：Hello Word
编辑距离：1
差异百分比：9.1%
发音正确率：90.9%
```

---

## 六、编译与运行

### 6.1 编译环境要求

| 项 | 要求 | 本机配置 |
|---|---|---|
| JDK | 17+ | Android Studio 自带 JBR (17) |
| Android SDK | Platform 36 + Build-Tools 34 | 已安装 |
| Gradle | 8.9 (自动下载) | 首次编译自动下载 |
| Android Studio | Koala+ (推荐) | 已安装 |

### 6.2 命令行编译

```powershell
# 设置 JAVA_HOME 指向 Android Studio 自带 JBR
$env:JAVA_HOME = "c:\Program Files\Android\Android Studio\jbr"
$env:PATH = "$env:JAVA_HOME\bin;$env:PATH"

# 进入项目目录
cd c:\Users\PC1\Documents\trae_projects\EnCnVoice

# 编译 debug APK
.\gradlew.bat assembleDebug
```

**编译产物路径：**
```
app\build\outputs\apk\debug\app-debug.apk
```

### 6.3 Android Studio 编译

1. 打开 Android Studio → Open → 选择 `EnCnVoice` 目录
2. 等待 Gradle Sync 完成
3. 点击 Run 按钮 (或 Shift+F10) 安装到模拟器/设备

### 6.4 安装到设备

```powershell
# USB 连接手机并开启 USB 调试，或启动模拟器
adb install "c:\Users\PC1\Documents\trae_projects\EnCnVoice\app-debug.apk"
```

---

## 七、交互流程

### 7.1 英文发音训练示例

1. 打开 App，语言选择默认"英文"
2. 在输入框输入 `Hello World`
3. 点击"播放标准音" → TTS 朗读英语标准发音
4. 点击"开始发音训练" → 系统提示"请开始朗读"
5. 对着麦克风朗读，系统识别为 `Hello Word` (少一个字母 l)
6. 结果区显示：编辑距离 1，差异百分比 9.1%，发音正确率 90.9%

### 7.2 中文发音训练示例

1. 语言选择切换为"中文"
2. 在输入框输入 `你好世界`
3. 点击"播放标准音" → TTS 朗读中文标准发音
4. 点击"开始发音训练" → 系统提示"请开始朗读"
5. 对着麦克风朗读，系统识别为 `你好世纪` (最后一个字不同)
6. 结果区显示：编辑距离 1，差异百分比 25.0%，发音正确率 75.0%

---

## 八、权限说明

| 权限 | 用途 | 申请方式 |
|---|---|---|
| `RECORD_AUDIO` | 麦克风录音，语音识别必需 | 动态申请 (Android 6.0+) |
| `INTERNET` | 语音识别服务需联网 (云端识别) | 静态声明 |
| `ACCESS_NETWORK_STATE` | 检测网络可用性 | 静态声明 |

---

## 九、依赖库

| 依赖 | 版本 | 用途 |
|---|---|---|
| `androidx.core:core-ktx` | 1.13.1 | Kotlin 扩展支持 |
| `androidx.appcompat:appcompat` | 1.7.0 | 向后兼容支持 |
| `com.google.android.material:material` | 1.12.0 | Material Design 组件 |
| `androidx.constraintlayout:constraintlayout` | 2.1.4 | 布局约束 (未直接使用，预留) |

---

## 十、已知限制与注意事项

1. **语音识别依赖云端服务**：`RecognizerIntent` 依赖设备上的语音识别服务（通常为 Google 云端识别），需要网络连接。离线状态下无法使用语音识别功能。

2. **TTS 语音数据**：部分设备可能未预装中文 TTS 语音数据。如果 `setLanguage()` 返回 `LANG_MISSING_DATA`，需在系统设置中下载对应语言的语音包。

3. **编辑距离算法局限**：Levenshtein 距离基于字符级别，对于音近但字不同的错误（如 "right" → "write"）会给出较低正确率，但这是发音内容对比的预期行为。算法不分析声纹/音色，仅对比发音内容的文字差异。

4. **`startActivityForResult` 弃用警告**：代码使用了已弃用的 `startActivityForResult` API（功能正常）。如需现代化，可迁移到 `ActivityResultLauncher`。

5. **AGP 与 compileSdk 兼容性**：AGP 8.7.2 官方测试到 compileSdk 35，本项目使用 36。编译会产生警告但不影响功能。如需消除警告，在 `gradle.properties` 添加 `android.suppressUnsupportedCompileSdk=36`。

6. **`local.properties` 不入版本控制**：该文件包含本机 SDK 路径，不同机器路径不同，不应提交到 Git。

---

## 十一、文件清单

| 文件 | 类型 | 说明 |
|---|---|---|
| `settings.gradle` | 配置 | Gradle 项目配置，声明仓库源和模块 |
| `build.gradle` (根) | 配置 | AGP 8.7.2 + Kotlin 2.0.21 插件声明 |
| `build.gradle` (app) | 配置 | compileSdk 36 + 依赖声明 |
| `gradle.properties` | 配置 | JVM 参数 + AndroidX 启用 |
| `local.properties` | 配置 | SDK 路径 (本机专属) |
| `gradlew` / `gradlew.bat` | 脚本 | Gradle Wrapper 启动脚本 |
| `gradle/wrapper/*` | 二进制 | Gradle 8.9 下载配置 + JAR |
| `app/proguard-rules.pro` | 规则 | ProGuard 混淆规则 (空) |
| `AndroidManifest.xml` | 清单 | 权限声明 + Activity 注册 |
| `MainActivity.kt` | 源码 | 核心逻辑 (TTS + 语音识别 + 编辑距离) |
| `activity_main.xml` | 布局 | 垂直布局 UI (RadioGroup + 输入框 + 按钮 + 结果区) |
| `strings.xml` | 资源 | 字符串资源 |
| `ic_launcher.xml` / `ic_launcher_round.xml` | 资源 | 矢量应用图标 |
| `app-debug.apk` | 产物 | 编译输出 APK (6.8 MB) |

---

## 十二、交付物清单

- [x] 完整 Android 项目源码 (Kotlin)
- [x] Gradle 构建配置 (Wrapper + AGP + 依赖)
- [x] 应用图标 (矢量，自适应)
- [x] 编译产物 `app-debug.apk` (可直接安装)
- [x] 代码详细注释 (语音识别逻辑 + 文本对比算法)
- [x] 本交付说明文档

---

*文档生成日期：2026-09-20*
*项目路径：c:\Users\PC1\Documents\trae_projects\EnCnVoice*
